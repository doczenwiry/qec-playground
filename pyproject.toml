[project]
name = "qec-playground"
description = "a collection of notebooks and stim circuits to study QEC"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "ipykernel>=7.3.0",
    "stim>=1.16.0",
]
